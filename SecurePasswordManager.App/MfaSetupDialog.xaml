<Window x:Class="SecurePasswordManager.App.MfaSetupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set Up Multi-Factor Authentication" Height="620" Width="520"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Scan QR code with your authenticator app, save recovery codes, then verify setup."
                   FontSize="13" TextWrapping="Wrap" Margin="0,0,0,10"/>

        <Border Grid.Row="1" BorderBrush="#D0D0D0" BorderThickness="1" Background="White" Padding="12" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="QR Code" FontWeight="Bold" Margin="0,0,0,8"/>
                <Image x:Name="QrCodeImage" Width="180" Height="180" Stretch="Fill" HorizontalAlignment="Left"/>
                <TextBlock Text="Manual setup key:" FontWeight="Bold" Margin="0,10,0,4"/>
                <TextBox x:Name="SecretTextBox" IsReadOnly="True" Padding="8" FontFamily="Consolas"/>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="2" Text="Recovery codes (store securely):" FontWeight="Bold" Margin="0,0,0,6"/>

        <Border Grid.Row="3" BorderBrush="#D0D0D0" BorderThickness="1" Background="White" Padding="8" Margin="0,0,0,10">
            <TextBox x:Name="RecoveryCodesTextBox"
                     IsReadOnly="True"
                     FontFamily="Consolas"
                     FontSize="13"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     BorderThickness="0"
                     Background="Transparent"/>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Verification code:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="VerifyCodeBox" Width="120" MaxLength="6" Padding="8" FontSize="16"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock x:Name="ErrorText" Foreground="#C62828" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <Button Content="Enable MFA" Width="100" Margin="0,0,8,0" Click="OnEnableMfa" IsDefault="True"/>
            <Button Content="Cancel" Width="90" Click="OnCancel" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>